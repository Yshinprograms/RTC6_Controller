#pragma once

// A simple structure to represent a 3D point in real-world units (e.g., mm).
struct Point {
    double x = 0.0;
    double y = 0.0;
    double z = 0.0; // Reserved for future 3D operations
};